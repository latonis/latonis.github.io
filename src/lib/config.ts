import { dev } from '$app/environment'

export const title = "Land of Jacob's Musings"
export const description = "Jacob's Musings in Tech"
export const url = dev ? 'http://localhost:5174/' : 'https://latonis.github.io/'
